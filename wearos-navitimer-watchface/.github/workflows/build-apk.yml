name: Build Wear OS APK
on:
  workflow_dispatch:
  push:
    branches: [wearos-navitimer-watchface]
    paths: ['wearos-navitimer-watchface/**']
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Build debug APK
        working-directory: wearos-navitimer-watchface
        run: gradle :app:assembleDebug --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: premium-wear-watchface-apk
          path: wearos-navitimer-watchface/app/build/outputs/apk/debug/app-debug.apk
